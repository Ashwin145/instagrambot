import os
import glob

cookie_del = glob.glob("config/*cookie.json")
os.remove(cookie_del[0])

from instabot import Bot

bot = Bot()
bot.login(username="stuart_broads183", password="********")
bot.follow("ashwin8741")
